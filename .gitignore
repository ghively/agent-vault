__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
# Coverage artifacts (pytest-cov; regenerated in CI)
.coverage
.coverage.*
htmlcov/
coverage.xml
build/
dist/
.venv/
# Built vault index (regenerated by `agent_vault.build_index` / the service)
_index.json
_index.md
# Full-text search sidecar (FTS5; regenerated by `agent_vault.search`)
_index.db
_index.db.tmp
# Semantic vector index (regenerated by `agent_vault.semantic build`)
_vectors.db
_vectors.db.tmp
# Local orchestration scratch (task briefs/reports/review packages)
.superpowers/
web/node_modules/
web/dist/
web/tsconfig.tsbuildinfo
# Runtime lock artifact (created by agent_vault.locking; safe to leave alone)
registry/_vault.lock
